apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-training-config
  labels:
    {{- include "fraud-training.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.trainingConfig | toPrettyJson | nindent 4 }}
